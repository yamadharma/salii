export PATH="$PATH:/bin:/sbin:/usr/bin:/usr/sbin:."
export SHELL="/bin/sh"
export TERM=linux
ulimit -c 0
umask 022

if [ $(id -u) -eq 0 ]
then                 
   export PS1="\h:\w \u\# "
else
   export PS1="\h:\w \u\$ " 
fi
