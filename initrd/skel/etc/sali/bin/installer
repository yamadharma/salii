#!/bin/ash

. /etc/init.d/functions
. /tmp/variables.txt

## Let's gather the given kernel arguments
p_separator
p_section "Starting installation process"

for file in $(ls -1 /etc/sali/installer.d/)
do
        . "/etc/sali/installer.d/${file}"
done
