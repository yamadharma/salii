
p_stage "Starting local syslogd server"

p_service "Starting syslogd"
/sbin/syslogd -O /tmp/si_monitor.log -S

p_service "Also starting klogd for kernel messages"
/sbin/klogd -c 2

p_service "Logging enabled"
