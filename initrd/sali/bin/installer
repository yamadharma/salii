#!/bin/ash

. /etc/init.d/functions
. /tmp/variables.txt

## Let's gather the given kernel arguments
p_separator
p_section "Starting installation process"

for file in $(ls -1 /etc/installer.d/)
do
        . "/etc/installer.d/${file}"
done
